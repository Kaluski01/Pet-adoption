module.exports = {
    // Indicates the root directory of your project
    rootDir: './src',
    // The test environment that will be used for testing
    testEnvironment: 'jsdom',
    // Adjust the glob patterns to match your test file naming convention and directory structure
    testMatch: ['<rootDir>/src/**/*.js', '<rootDir>/src/**/*.jsx'],
  };
  